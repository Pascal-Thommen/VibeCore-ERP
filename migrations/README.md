# Database Migrations

Alembic migrations will live here. Every schema change must be deterministic,
reversible where safely possible, and executable against a clean database.

Changes to `shell` require migrations. Changes to `core` additionally require
explicit architecture-owner approval under `.ai/ARCHITECTURE.md`.
